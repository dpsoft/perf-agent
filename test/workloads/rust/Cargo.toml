[package]
name = "rust-workload"
version = "0.1.0"
edition = "2021"

[dependencies]
num_cpus = "1.16"
libc = "0.2"

# Include debug symbols in release builds for profiling
[profile.release]
debug = true
strip = false
